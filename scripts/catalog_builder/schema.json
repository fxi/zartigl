{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Zartigl Catalog",
  "type": "object",
  "required": ["schemaVersion", "generatedAt", "layers"],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "generatedAt": { "type": "string" },
    "layers": {
      "type": "array",
      "items": { "$ref": "#/$defs/Layer" },
      "minItems": 1
    }
  },
  "$defs": {
    "Layer": {
      "type": "object",
      "required": ["id", "label", "category", "kind", "dataset", "stores", "variables", "dimensions", "defaults"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "label": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "category": { "type": "string", "minLength": 1 },
        "tags": { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "kind": { "type": "string", "enum": ["vector", "scalar"] },
        "dataset": { "$ref": "#/$defs/Dataset" },
        "stores": { "$ref": "#/$defs/Stores" },
        "variables": { "$ref": "#/$defs/Variables" },
        "dimensions": { "$ref": "#/$defs/Dimensions" },
        "defaults": { "$ref": "#/$defs/Defaults" }
      }
    },
    "Dataset": {
      "type": "object",
      "required": ["id"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "provider": { "type": "string", "minLength": 1 },
        "productId": { "type": "string", "minLength": 1 }
      }
    },
    "Stores": {
      "type": "object",
      "required": ["field"],
      "additionalProperties": false,
      "properties": {
        "field": {
          "type": "object",
          "required": ["type", "url", "layout"],
          "additionalProperties": false,
          "properties": {
            "type": { "type": "string", "const": "zarr" },
            "url": { "type": "string", "pattern": "^https?://" },
            "layout": { "type": "string", "const": "time-chunked" }
          }
        },
        "pointSeries": {
          "type": "object",
          "required": ["type", "url", "layout"],
          "additionalProperties": false,
          "properties": {
            "type": { "type": "string", "const": "zarr" },
            "url": { "type": "string", "pattern": "^https?://" },
            "layout": { "type": "string", "const": "geo-chunked" }
          }
        },
        "wmts": { "$ref": "#/$defs/Wmts" }
      }
    },
    "Variables": {
      "oneOf": [
        {
          "type": "object",
          "required": ["kind", "value"],
          "additionalProperties": false,
          "properties": {
            "kind": { "type": "string", "const": "scalar" },
            "value": { "type": "string", "minLength": 1 },
            "standardName": { "type": "string" },
            "units": { "type": "string" }
          }
        },
        {
          "type": "object",
          "required": ["kind"],
          "additionalProperties": false,
          "properties": {
            "kind": { "type": "string", "const": "vector" },
            "u": { "type": "string", "minLength": 1 },
            "v": { "type": "string", "minLength": 1 },
            "derivation": { "$ref": "#/$defs/VectorDerivation" },
            "standardName": { "type": "string" },
            "units": { "type": "string" }
          },
          "oneOf": [
            { "required": ["u", "v"] },
            { "required": ["derivation"] }
          ]
        }
      ]
    },
    "VectorDerivation": {
      "type": "object",
      "required": ["kind", "direction_variable", "magnitude_variable", "direction_convention", "output_direction"],
      "additionalProperties": false,
      "properties": {
        "kind": { "type": "string", "enum": ["direction_magnitude"] },
        "direction_variable": { "type": "string", "minLength": 1 },
        "magnitude_variable": { "type": "string", "minLength": 1 },
        "direction_convention": { "type": "string", "enum": ["from", "toward"] },
        "output_direction": { "type": "string", "enum": ["from", "toward"] }
      }
    },
    "Wmts": {
      "type": "object",
      "required": ["capabilities_url", "base_url", "layer", "tileMatrixSet", "format"],
      "additionalProperties": false,
      "properties": {
        "capabilities_url": { "type": "string", "pattern": "^https?://" },
        "base_url": { "type": "string", "pattern": "^https?://" },
        "layer": { "type": "string", "minLength": 1 },
        "tileMatrixSet": { "type": "string", "minLength": 1 },
        "format": { "type": "string", "minLength": 1 },
        "style": { "type": "string", "minLength": 1 }
      }
    },
    "Dimensions": {
      "type": "object",
      "required": ["time"],
      "additionalProperties": false,
      "properties": {
        "time": { "$ref": "#/$defs/TimeDimension" },
        "vertical": { "$ref": "#/$defs/VerticalDimension" },
        "latitude": { "$ref": "#/$defs/SpatialDimension" },
        "longitude": { "$ref": "#/$defs/SpatialDimension" }
      }
    },
    "TimeDimension": {
      "type": "object",
      "required": ["size"],
      "additionalProperties": false,
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "step": { "type": "number", "exclusiveMinimum": 0 },
        "values": { "type": "array", "items": { "type": "number" } },
        "size": { "type": "integer", "minimum": 1 },
        "chunkSize": { "type": "integer", "minimum": 1 },
        "units": { "type": "string" }
      }
    },
    "VerticalDimension": {
      "type": "object",
      "required": ["label"],
      "additionalProperties": false,
      "properties": {
        "label": { "type": "string", "minLength": 1 },
        "min": { "type": "number" },
        "max": { "type": "number" },
        "step": { "type": "number" },
        "values": { "type": "array", "items": { "type": "number" } },
        "size": { "type": "integer", "minimum": 1 },
        "chunkSize": { "type": "integer", "minimum": 1 },
        "units": { "type": "string" }
      }
    },
    "SpatialDimension": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "step": { "type": "number" },
        "values": { "type": "array", "items": { "type": "number" } },
        "size": { "type": "integer", "minimum": 1 },
        "chunkSize": { "type": "integer", "minimum": 1 },
        "units": { "type": "string" }
      }
    },
    "Defaults": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "backend": { "type": "string", "enum": ["zarr", "wmts"] },
        "palette": { "type": "string" },
        "particles": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "density": { "type": "number", "exclusiveMinimum": 0 },
            "speed": { "type": "number", "exclusiveMinimum": 0 },
            "fadeOpacity": { "$ref": "#/$defs/ZoomWeighted" },
            "dropRate": { "type": "number", "minimum": 0 },
            "dropRateBump": { "type": "number", "minimum": 0 }
          }
        },
        "raster": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "opacity": { "type": "number", "minimum": 0, "maximum": 1 },
            "logScale": { "type": "boolean" },
            "vibrance": { "type": "number", "minimum": -1, "maximum": 1 }
          }
        }
      }
    },
    "ZoomWeighted": {
      "oneOf": [
        { "type": "number" },
        {
          "type": "array",
          "items": { "type": "number" },
          "minItems": 2,
          "maxItems": 2
        }
      ]
    }
  }
}
