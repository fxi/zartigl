[project]
name = "zartigl-scripts"
version = "0.2.0"
description = "Data preparation scripts for zartigl"
requires-python = ">=3.12"
dependencies = [
    "copernicusmarine>=2.0",
]

[project.scripts]
build-catalog = "build_catalog:main"
